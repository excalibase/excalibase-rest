apiVersion: v1
kind: Service
metadata:
  name: {{ include "excalibase-rest.fullname" . }}
  labels:
    {{- include "excalibase-rest.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "excalibase-rest.selectorLabels" . | nindent 4 }}
